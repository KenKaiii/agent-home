{
  "name": "@agent-home/relay",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute relay-db --local --file=migrations/0001_init.sql",
    "db:migrate:remote": "wrangler d1 execute relay-db --remote --file=migrations/0001_init.sql"
  },
  "dependencies": {
    "@agent-home/protocol": "*",
    "hono": "^4.7.0",
    "jose": "^6.0.0",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250320.0",
    "typescript": "~5.9.2",
    "wrangler": "^4.0.0"
  }
}
